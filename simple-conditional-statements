#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int num1,num2;
	printf("enter 2 numbers");
	scanf("%d%d",&num1,&num2);
	if(num1<num2){
		printf("biggt number  %d",num2);
	}else{
		printf("biggest number is %d",num1);
	}
	return EXIT_SUCCESS;
}
